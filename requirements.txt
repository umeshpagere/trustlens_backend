# NOTE: The +cpu index URL below is only needed in Docker (Linux/CPU builds).
# On Mac (Apple Silicon or Intel), remove the line below if pip cannot resolve torch.
# --extra-index-url https://download.pytorch.org/whl/cpu

# Core Web Framework
flask[async]==3.1.2
flask-cors==6.0.2
gunicorn==23.0.0

# AI/ML - CPU Optimized
# torch 2.1.0+cpu is a Linux/Docker-only build. For local Mac use, pip will
# resolve the correct version automatically (2.6+ for Python 3.13).
torch
sentence-transformers==3.0.1
# transformers 4.41.2 has no Python 3.13 wheel — unpin
transformers
# scikit-learn 1.4.1 has no prebuilt wheel for Python 3.13/ARM64 — unpin
scikit-learn
# numpy 1.26 is incompatible with torch 2.6+; allow pip to resolve
numpy
# scipy 1.12.0 requires OpenBLAS which is absent on this Mac — unpin to use prebuilt wheel
scipy
nltk==3.9.1
# spacy 3.8.4 has no wheel for Python 3.13/ARM64 — unpin
spacy
# huggingface_hub 0.29.0 may conflict with newer transformers — unpin
huggingface_hub

# Database & Environment
pydantic==2.10.6
python-dotenv==1.0.1
pymongo==4.10.1
certifi
httpx

# Video & Image Processing
ffmpeg-python==0.2.0
ImageHash==4.3.1
Pillow==11.1.0
opencv-python-headless==4.11.0.86
yt-dlp
youtube-transcript-api==0.6.3

# Networking & External APIs
requests==2.32.3
deepgram-sdk==3.2.1
openai==1.61.1
google-genai==1.62.0
google-cloud-speech==2.37.0
azure-cognitiveservices-vision-computervision==0.9.1
msrest==0.7.1
anyio==4.8.0

# Search & Utilities
beautifulsoup4==4.12.3
googlesearch-python==1.3.0
tenacity==9.0.0
langdetect
spacy-transformers
newspaper3k
lxml_html_clean
pandas
